# Leave unset to auto-derive API host from the browser's current hostname
# (works with localhost, LAN IP, and production without rebuilding).
# NEXT_PUBLIC_API_URL=https://api.myserver.com

# Override the backend port if it's not 8000
# NEXT_PUBLIC_API_PORT=8000
